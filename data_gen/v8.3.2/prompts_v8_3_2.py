# prompts_v8_3_2.py
# 两阶段数据合成提示词
# 说明：第一阶段只生成 input；第二阶段只根据 instruction + input 生成 output。
# 目的：避免 output 参考 task_seeds / role / scenario 等隐藏信息，修复 seed 泄漏问题。

INPUT_PROMPT = """
# Role
你是一个高水平的数据合成机器人。你的任务是根据给定岗位、场景和任务种子，只生成一段真实员工随手记录的原始工作流水 input。

# 重要定位
你现在只负责生成 input，不生成周报，不生成总结，不生成 output。
input 是用户的原始记录，不是任务指令、不是周报、不是结构化总结。

# 生成目标
生成一段真实、自然、非结构化的工作碎片记录。

# 必须满足
1. 口语化，像真实员工随手记或语音转写，不要正式书面语。
2. 可以有少量犹豫词、情绪词、自我纠正，例如“额”“有点烦”“哦不对”“先这样”。
3. 可以有轻微错别字、同音字或 ASR 风格错误，但不要过量，不要影响核心事实识别。
4. 内容可以混合包含：已完成事项、进行中事项、问题/风险、待确认内容、后续计划、临时插入事项。
5. 允许逻辑跳跃、顺序混乱、信息不完整、句子不完整。
6. 可以不分段，也可以是 1-4 个短段，但绝不能写成结构化总结。
7. 原始记录中应自然出现不同状态边界，例如“已经做完”“还在排查”“明天准备”“等客户确认”“可能是某个原因”。
8. 工作内容占主体，生活吐槽或低价值琐事只能少量点缀。
9. 可以出现同一事项的前后修正，例如“哦不对，这个还没发版，只是测完了”。
10. 可以出现不确定表达，例如“好像”“大概”“可能”“先看下”“还没定”。

# 严格禁止
1. 不要出现任何指令性文字，如“请帮我整理”“生成周报”“风格要求”“当前周报风格”。
2. 不要出现标题、小标题、分点、序号、表格。
3. 不要写成“本周完成 / 下周计划 / 风险”这种总结格式。
4. 不要显得像 AI 在整理内容。
5. 不要主动补全完整背景，不要把所有事情说得特别清楚。
6. 不要把所有事项都写成已完成，必须保留真实工作中常见的进行中、待确认、阻塞和计划状态。

# 输入条件
岗位：{role}
触发场景：{scenario}
核心任务种子：
{task_seeds}
日常琐事：{routine_task}
时间表达模式：{temporal_pattern}
姓名提及概率提示：{name_mention_prob}
错别字/ASR噪声概率提示：{typo_noise_prob}
情绪化表达概率提示：{emotion_noise_prob}
信息不完整概率提示：{incomplete_info_prob}

# Output JSON Format
严格输出 JSON 对象，仅包含 input 字段：
{{
  "input": "用户原始工作流水"
}}
"""

COMMON_OUTPUT_RULES = """
# Role
你是一名严格忠实于原始记录的工作记录整理员。你的任务是把用户提供的原始工作流水整理为基础周报初稿。

# 绝对重要
你只能根据下面提供的 input 生成 output。
你看不到、也不允许假设任何任务种子、岗位背景、隐藏资料或外部信息。
所有 output 内容都必须能从 input 中找到直接依据。

# 核心目标
1. 保持事实忠实：不得新增 input 中没有的任务、结果、指标、负责人、时间节点、协同对象或业务价值。
2. 保持信息完整：尽量覆盖 input 中的关键任务、进展、问题、风险、待办和计划。
3. 保持状态边界：不得把“计划做/准备做/待确认/排查中”写成“已完成/已解决/已落地”。
4. 保留不确定性：input 中的“可能、大概、好像、待确认、还没定”等信息，output 中也必须保持相应不确定性。
5. 完成结构化整理：按工作模块、事项类型或项目主题归类，而不是机械复述原始顺序。
6. 完成职业化表达：去除口头禅、情绪词和明显噪声，将口语转为正式、清晰、凝练的书面表达。
7. 输出应是中性周报初稿，不是高情商包装版汇报。

# 状态与确定性映射
| 原文表达 | 输出状态 |
| :--- | :--- |
| 完成了、修好了、上线了、提交了、整理完了、验收通过 | 已完成 |
| 正在做、还在看、继续排查、持续跟进、测试中、联调中 | 进行中 |
| 准备做、打算做、计划下周、明天开始、后面要做 | 计划推进 |
| 待确认、还没定、等反馈、看客户怎么说、技术还没回复 | 待确认 |
| 卡住了、报错、阻塞、没跑通、失败、延期 | 待解决 / 存在问题 |
| 可能、大概、好像、初步判断、暂时不确定 | 保留不确定性 |

# 禁止状态前推
- “准备处理”不能写成“已启动处理机制”或“已完成处理”。
- “排查中”不能写成“已定位并解决”。
- “待客户确认”不能写成“已与客户达成一致”。
- “可能是配置问题”不能写成“确认由配置问题导致”。
- “得想想办法”“后面看看”只能写成“处理方式待确认/需继续跟进”，不能写成“制定方案/启动机制”。

# 问题与风险处理
- input 中出现的问题、阻塞、延迟、失败，必须如实保留。
- input 提到影响范围时，才写影响范围。
- input 未提影响范围时，写“影响范围待明确”，不得自行扩大。
- input 明确提到应对动作时，才写具体后续处理。
- input 未提具体应对动作时，写“后续处理方式待确认”或“需继续跟进”，不得编造方案。
- input 未明确下周计划时，下周计划写“暂无明确记录”，不得从问题中自动推导具体计划。

# 禁止包装与套话
- 禁止使用“显著提升、全面优化、重大突破、圆满完成、深度赋能、形成机制、有效保障”等无依据高级表达。
- 禁止使用“本周主要围绕……开展工作”“按计划推进”“聚焦于”等模板套话。
- 禁止输出“说明与建议”“心得体会”“价值复盘”“思考与复盘”“请领导审阅”“祝周末愉快”。

【待补充信息规则】
只有当 input 中存在核心工作信息缺失、状态不明、结果待确认、影响范围不明、后续安排不清等情况时，才输出“待补充信息”栏目。
如果没有明确需要补充的信息，必须直接省略整个“待补充信息”栏目。
禁止输出“暂无明确待补充信息”“暂无待补充信息”“当前记录覆盖主要内容”等占位句。

# 自检清单
输出前必须检查：
1. 是否新增了 input 没有的信息？有则删除。
2. 是否将计划、待确认、排查中前推为已完成、已解决？有则改回。
3. 是否将可能、大概、好像写成确定结论？有则恢复不确定性。
4. 是否编造了处理方案、协同对象、下周计划或业务价值？有则删除。
5. 是否遗漏了 input 中真实存在的问题、阻塞、失败或延期？有则补回。
"""

TABLE_OUTPUT_PROMPT = COMMON_OUTPUT_RULES + """
# 输出格式要求：表格风格
主体内容必须使用 Markdown 表格呈现。

# 用户指令
{instruction}

# 原始工作流水 input
{input_text}

# 表格风格周报模板
# 工作周报

## 本周工作概览
用 2-4 句话客观概括本周主要工作、当前进展、存在问题和后续方向。只能概括 input 中已有信息，不得拔高价值或补充无依据成果。

## 本周工作明细
| 工作模块 | 工作内容 | 当前状态 | 依据说明 |
| :--- | :--- | :--- | :--- |
| 模块名称 | 用正式语言概括具体工作内容 | 已完成 / 进行中 / 待确认 / 待解决 / 计划推进 / 待验证 | 简要说明该状态来自 input 中哪类表达 |

## 问题与风险
| 问题/风险 | 当前状态 | 影响或表现 | 后续处理 |
| :--- | :--- | :--- | :--- |
| 问题标题 | 待排查 / 待确认 / 进行中 / 已处理 | 只写 input 支撑的影响；没有则写“影响范围待明确” | 只写 input 提到的处理动作；没有则写“后续处理方式待确认” |

## 后续计划
| 计划事项 | 关联工作 | 目标或处理方向 | 状态 |
| :--- | :--- | :--- | :--- |
| 若 input 有明确计划则填写；若没有明确计划则只写一行“暂无明确记录” | - | - | - |

## 待补充信息
仅当存在核心信息缺失时才输出本栏目。

| 待补充项 | 原因 |
| :--- | :--- |
| 只列 input 中确实无法确认、且影响周报理解的核心缺失信息 | 说明为什么需要补充，不能新增事实 |

如果没有核心缺失信息，必须省略整个“待补充信息”栏目，不要输出任何“暂无”占位内容。

# Output JSON Format
严格输出 JSON 对象，仅包含 output 字段：
{{
  "output": "Markdown 格式的表格风格周报初稿"
}}
"""

LIST_OUTPUT_PROMPT = COMMON_OUTPUT_RULES + """
# 输出格式要求：列表风格
主体内容必须使用 Markdown 标题和项目符号列表呈现，不得使用 Markdown 表格。

# 用户指令
{instruction}

# 原始工作流水 input
{input_text}

# 列表风格周报模板
# 工作周报

## 本周工作概览
- 用 2-4 条客观概括本周主要工作、当前进展、存在问题和后续方向。
- 只能概括 input 中已有信息，不得拔高价值或补充无依据成果。

## 本周工作明细
- **工作主题**：说明具体工作内容和当前状态。状态只能使用“已完成 / 进行中 / 待确认 / 待解决 / 计划推进 / 待验证”等保守表述。

## 问题与风险
- **问题标题**：说明问题现象、当前状态和影响范围。没有明确影响时写“影响范围待明确”，不要自行扩大。
  - 后续处理：只写 input 中提到的处理动作；没有则写“后续处理方式待确认”。

## 后续计划
- 若 input 有明确计划，则整理为计划事项。
- 若 input 没有明确下周或后续计划，只写“暂无明确记录”，不要自行推导具体计划。

## 待补充信息
- 只列核心工作相关且确实缺失的信息；如果没有核心缺失信息，必须省略整个“待补充信息”栏目，不要输出“暂无明确待补充信息”等占位内容。
- 不要为低价值琐事补充信息。

# Output JSON Format
严格输出 JSON 对象，仅包含 output 字段：
{{
  "output": "Markdown 格式的列表风格周报初稿"
}}
"""

OUTPUT_PROMPT_MAP = {
    "from": TABLE_OUTPUT_PROMPT,
    "list": LIST_OUTPUT_PROMPT,
}

